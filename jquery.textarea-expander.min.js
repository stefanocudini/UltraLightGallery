/**
 * Usage:
 *
 * From JavaScript, use:
 *     $(<node>).TextAreaExpander(<minHeight>, <maxHeight>);
 *     where:
 *       <node> is the DOM node selector, e.g. "textarea"
 *       <minHeight> is the minimum textarea height in pixels (optional)
 *       <maxHeight> is the maximum textarea height in pixels (optional)
 *
 *     or Assign a class of "expand" to any <textarea> tag.
 */
(function(b){b.fn.TextAreaExpander=function(e,f){function c(a){var a=a.target||a,b=a.value.length,c=a.offsetWidth;if(b!=a.valLength||c!=a.boxWidth){if(g&&(b<a.valLength||c!=a.boxWidth))a.style.height="0px";var d=Math.max(a.expandMin,Math.min(a.scrollHeight,a.expandMax));a.style.overflow=a.scrollHeight>d?"auto":"hidden";a.style.height=d-7+"px";a.valLength=b;a.boxWidth=c}return!0}var g=!(b.browser.msie||b.browser.opera);this.each(function(){if("textarea"==this.nodeName.toLowerCase()){var a=this.className.match(/expand(\d+)\-*(\d+)*/i);
this.expandMin=e||(a?parseInt("0"+a[1],10):0);this.expandMax=f||(a?parseInt("0"+a[2],10):99999);c(this);this.Initialized||(this.Initialized=!0,b(this).bind("keyup",c).bind("focus",c))}});return this}})(jQuery);jQuery(document).ready(function(){jQuery("textarea[class*=expand]").TextAreaExpander()}); 
